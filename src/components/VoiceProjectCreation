import React from 'react';
import { FaMicrophone } from 'react-icons/fa';

const VoiceProjectCreation = ({ onProjectCreated }) => {
  return (
    <div style={{ marginBottom: '20px' }}>
      <button 
        style={{
          display: 'flex',
          alignItems: 'center',
          backgroundColor: '#512da8',
          color: 'white',
          border: 'none',
          borderRadius: '5px',
          padding: '10px 20px',
          cursor: 'pointer',
          fontSize: '14px',
          fontWeight: 'bold'
        }}
        onClick={() => console.log("Voice Project Creation clicked")}
      >
        <FaMicrophone style={{ marginRight: '8px' }} /> Create project by Voice
      </button>
    </div>
  );
};

export default VoiceProjectCreation;